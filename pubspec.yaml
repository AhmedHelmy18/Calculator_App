name: calculate_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_native_splash: ^2.4.1

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter



flutter_native_splash:
  color: "#000000"
  image: asset/appstore.png
  android_gravity: center
  android: true
  ios: true
  android_12:
    image: asset/appstore.png
    icon_background_color: "#000000"
    fullscreen: true #

flutter:
  uses-material-design: true
  assets:
    - asset/playstore.png  # Ensure this path matches the file
